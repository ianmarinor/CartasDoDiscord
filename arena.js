import { emptyObj } from './script.js';


export const are = document.getElementById('are')
export let areObj


function Main(){


    areObj = [
        emptyObj,
        emptyObj,
        emptyObj,
        emptyObj,
        emptyObj,
        emptyObj,
        emptyObj,
        emptyObj,
        emptyObj,
        emptyObj,
      ];
     























return console.log('----------------- **LOADED** ---------------------');
}

window.addEventListener('load', Main)